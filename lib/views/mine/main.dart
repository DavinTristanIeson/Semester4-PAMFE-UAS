import 'package:flutter/widgets.dart';

class MyFlashcardsView extends StatelessWidget {
  const MyFlashcardsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}